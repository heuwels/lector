// Custom event for same-tab localStorage changes (storage event only fires cross-tab)
export const LANGUAGE_CHANGE_EVENT = 'lector-language-change';

/** Same-tab notification that the reader typography settings changed (#570). */
export const PROSE_STYLE_CHANGE_EVENT = 'lector-prose-style-change';
