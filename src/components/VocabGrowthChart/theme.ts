// Recharts grid/axis/legend colours, retinted to the grown-up palette
// (concrete hex — recharts SVG props don't reliably resolve CSS vars).
export const darkChartTheme = {
  grid: '#352f24', // border (dark)
  axis: '#a99e8a', // muted-foreground (dark)
  legend: '#ece5d6', // foreground (dark)
};

export const lightChartTheme = {
  grid: '#e9e1d0', // border (light)
  axis: '#6b6356', // muted-foreground (light)
  legend: '#2c2a23', // foreground (light)
};
