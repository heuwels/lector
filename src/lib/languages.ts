// Re-export of the shared language registry (`languages/registry.ts`), the single
// source of truth shared with the Hono API. Add or edit a language there.
export * from '../../languages/registry';
